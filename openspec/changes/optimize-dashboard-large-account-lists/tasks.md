# Tasks

- [x] Add regression coverage for large account arrays on dashboard account cards, donut charts, account list, multi-select filters, account assignment picker, and routing settings picker.
- [x] Cap dashboard account card rendering and remove unbounded per-index animation delay.
- [x] Aggregate donut chart account slices and legend rows beyond a bounded top set while preserving total math.
- [x] Cap accounts-page list rendering with an explicit incremental reveal path.
- [x] Cap dropdown/menu/select account options while keeping search and selected values usable.
- [x] Run focused frontend tests, typecheck/lint where feasible, OpenSpec validation if the CLI is available, and commit atomically.
